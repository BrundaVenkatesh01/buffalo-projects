# ═══════════════════════════════════════════════════════════════════
# Buffalo Projects - Test Environment Configuration
# ═══════════════════════════════════════════════════════════════════
#
# This file is used by Vitest during test execution.
# These are dummy values for testing - they are never used with real Firebase.
#
# ═══════════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────────────┐
# │ Firebase Test Configuration (Required for tests)                │
# └─────────────────────────────────────────────────────────────────┘
# These values are mocked by src/test/setup/firebase-mocks.ts
# They just need to be present to satisfy environment checks

NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyTest_MockApiKey_ForTestingOnly123456789
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=test-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=test-project-buffalo
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=test-project-buffalo.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:test123456789abc
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-TESTTEST

# Firebase Emulator (disabled for unit tests)
NEXT_PUBLIC_FIREBASE_EMULATOR=0

# ┌─────────────────────────────────────────────────────────────────┐
# │ AI Integration (Optional for most tests)                        │
# └─────────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_GEMINI_API_KEY=test-gemini-api-key-for-mocking

# ┌─────────────────────────────────────────────────────────────────┐
# │ Analytics & Monitoring (Disabled in tests)                      │
# └─────────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_ERROR_TRACKING=false
NEXT_PUBLIC_POSTHOG_KEY=test-posthog-key
NEXT_PUBLIC_POSTHOG_HOST=https://test.posthog.com
NEXT_PUBLIC_ANALYTICS_ID=G-TESTTEST
NEXT_PUBLIC_SENTRY_DSN=https://test@test.ingest.sentry.io/test
NEXT_PUBLIC_SENTRY_ENVIRONMENT=test
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.0
NEXT_PUBLIC_SENTRY_REPLAY_SAMPLE_RATE=0.0
NEXT_PUBLIC_SENTRY_REPLAY_ERROR_SAMPLE_RATE=0.0

# ┌─────────────────────────────────────────────────────────────────┐
# │ Site Configuration                                              │
# └─────────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ┌─────────────────────────────────────────────────────────────────┐
# │ Feature Flags (Enabled for test coverage)                       │
# └─────────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_ENABLE_COMMENTS=true
NEXT_PUBLIC_ENABLE_MENTOR_MODE=true
NEXT_PUBLIC_ENABLE_GROUPS=false

# ┌─────────────────────────────────────────────────────────────────┐
# │ Server-Side Configuration (For API route tests)                 │
# └─────────────────────────────────────────────────────────────────┘
# These are test values and won't connect to real services
UPSTASH_REDIS_REST_URL=https://test-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=test-token
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW=60000
SESSION_SECRET=test-session-secret-for-unit-tests-only

# Email (disabled in tests)
EMAIL_PROVIDER=sendgrid
EMAIL_API_KEY=test-email-api-key
EMAIL_FROM_ADDRESS=test@buffaloprojects.com
EMAIL_FROM_NAME=Buffalo Projects Test

# ═══════════════════════════════════════════════════════════════════
# Notes
# ═══════════════════════════════════════════════════════════════════
#
# 1. This file is loaded by Vitest automatically via vitest.config.ts
# 2. All Firebase calls are mocked via src/test/setup/firebase-mocks.ts
# 3. These values never touch real services
# 4. NODE_ENV is automatically set to 'test' by Vitest
# 5. VITEST=true is automatically set by Vitest
#
# ═══════════════════════════════════════════════════════════════════
